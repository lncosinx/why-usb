--- driver/src/device.cpp
+++ driver/src/device.cpp
@@ -216,13 +216,7 @@
         0
     );

     if (!NT_SUCCESS(status)) {
-        ZwClose(processHandle);
         CloseSharedResources(Context);
         return status;
     }
@@ -232,15 +226,14 @@
         NtCurrentProcess(),
         Context->TxEventHandle,
         processHandle,
         &userTxEventHandle,
         EVENT_MODIFY_STATE | SYNCHRONIZE,
         0,
         0
     );

     if (!NT_SUCCESS(status)) {
-        ZwClose(userSectionHandle);
-        ZwClose(processHandle);
+        // Cannot safely ZwClose userSectionHandle here if not in user process context
         CloseSharedResources(Context);
         return status;
     }
@@ -250,17 +243,14 @@
         NtCurrentProcess(),
         Context->RxEventHandle,
         processHandle,
         &userRxEventHandle,
         EVENT_MODIFY_STATE | SYNCHRONIZE,
         0,
         0
     );

     if (!NT_SUCCESS(status)) {
-        ZwClose(userTxEventHandle);
-        ZwClose(userSectionHandle);
-        ZwClose(processHandle);
+        // Cannot safely ZwClose user handles here if not in user process context
         CloseSharedResources(Context);
         return status;
     }
